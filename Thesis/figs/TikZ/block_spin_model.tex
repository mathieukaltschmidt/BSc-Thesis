\documentclass[tikz]{standalone}
\input{fig_settings}
\begin{document}

\begin{tikzpicture}

%Outer Square 
\draw[-, thick] (0,0) rectangle (4,4);

%Outer Mean 
\draw[dashed, thick, color = gray] (-0.3,-0.3) rectangle (4.3,4.3);

%First Inner Separation
\draw[-, thick] (2,0) to (2,4);
\draw[-, thick] (0,2) to (4,2);

%Drawing The Smaller Mean Squares
\foreach \x in {0.1,1.1,2.1,3.1}
{   \foreach \y in {0.1,1.1,2.1,3.1}
    {   \draw[dashed, thick, color = gray] (\x,\y) to (\x+0.8, \y);
    	\draw[dashed, thick, color = gray] (\x,\y) to (\x, \y +0.8);
    	\draw[dashed, thick, color = gray] (\x,\y+0.8) to (\x+0.8, \y+0.8);
    	\draw[dashed, thick, color = gray] (\x+0.8,\y+0.8) to (\x+0.8, \y);
    }
}

%Particle Representations
\foreach \x in {0.3,1.3,2.3,3.3}
{   \foreach \y in {0.3,1.3,2.3,3.3}
   {   \draw[thick, fill = BScGreen] (\x,\y) circle(0.1);
    	\draw[thick, fill = BScGreen] (\x,\y + 0.4) circle(0.1);
      	\draw[thick, fill = BScGreen] (\x + 0.4,\y) circle(0.1); 
      	\draw[thick, fill = BScGreen] (\x + 0.4,\y + 0.4) circle(0.1);   	
    }
}

\end{tikzpicture}
\end{document} 